# Sample Data

El archivo `datos_prueba_favarcia.xlsx` no está incluido en el repositorio
por límites de tamaño de GitHub (~900KB).

## Cómo generarlo

Desde la raíz del proyecto:

```bash
python generar_datos_prueba.py
```

Esto genera el archivo automáticamente en esta carpeta (MODO = "demo").
